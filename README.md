# Simple Logger Class

A simple and customizable logger class for Node.js applications with color-coded log levels. Supports `info`, `warn`, `error`, and `success` log levels, making it easy to format your log messages.

## Features

- 🌟 `info` - Standard log messages.
- ⚠️ `warn` - Warning log messages.
- ❌ `error` - Error log messages.
- ✅ `success` - Success log messages.
- Lightweight and easy to use.
- Color-coded log messages for easier reading.

## Installation

To install the logger class via npm, run the following command:

```bash
npm install zyralog
